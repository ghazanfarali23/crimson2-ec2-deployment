# Local development / reference only
# For EC2 deployment, set these in the AWS Console → EC2 → User data script
# or export them before running the app locally.

COGNITO_USER_POOL_ID=your_pool_id_here
COGNITO_USER_POOL_CLIENT_ID=your_client_id_here
COGNITO_REGION=us-east-1
